@page "/admin/accounts"
@using FinanceBank.Models
@using FinanceBank.Data
@using Microsoft.EntityFrameworkCore
@inject IDbContextFactory<BFASDbContext> DbContextFactory
@layout AdminLayout

<div class="section-header">
    <h2 class="section-title">Bank Accounts</h2>
    <button class="btn btn-primary">+ Create Account</button>
</div>

<!-- Stats -->
<div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 24px;">
    <div style="background: linear-gradient(135deg, #E8F0E8, #D4E6D4); border-radius: 16px; padding: 20px; border: 2px solid #A9B388;">
        <div style="font-size: 13px; color: #2F3E2A; font-weight: 600;">Total Accounts</div>
        <div style="font-size: 28px; font-weight: 700; color: #5F6F52;">856</div>
    </div>
    <div style="background: linear-gradient(135deg, #D4E6D4, #BCC9A6); border-radius: 16px; padding: 20px; border: 2px solid #A9B388;">
        <div style="font-size: 13px; color: #2F3E2A; font-weight: 600;">Active</div>
        <div style="font-size: 28px; font-weight: 700; color: #5F6F52;">832</div>
    </div>
    <div style="background: linear-gradient(135deg, #F0F2EC, #DDE6D5); border-radius: 16px; padding: 20px; border: 2px solid #A9B388;">
        <div style="font-size: 13px; color: #2F3E2A; font-weight: 600;">Total Balance</div>
        <div style="font-size: 28px; font-weight: 700; color: #5F6F52;">₱ 2.4M</div>
    </div>
    <div style="background: linear-gradient(135deg, #E8F0E8, #D4E6D4); border-radius: 16px; padding: 20px; border: 2px solid #A9B388;">
        <div style="font-size: 13px; color: #2F3E2A; font-weight: 600;">This Month</div>
        <div style="font-size: 28px; font-weight: 700; color: #5F6F52;">+45</div>
    </div>
</div>

<!-- Accounts Table -->
<div style="background: white; border-radius: 16px; padding: 24px; border: 1px solid var(--border);">
    <table style="width: 100%; border-collapse: collapse;">
        <thead>
            <tr style="border-bottom: 1px solid var(--border);">
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">ACCOUNT NUMBER</th>
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">OWNER</th>
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">TYPE</th>
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">BALANCE</th>
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">STATUS</th>
                <th style="padding: 12px; text-align: left; font-size: 12px; font-weight: 600; color: var(--text-light);">ACTIONS</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var account in accounts)
            {
                <tr style="border-bottom: 1px solid var(--border);">
                    <td style="padding: 16px; font-weight: 600; font-family: monospace;">@account.AccountNumber</td>
                    <td style="padding: 16px;">@(account.Customer != null ? account.Customer.FullName : "N/A")</td>
                    <td style="padding: 16px;">@account.Currency</td>
                    <td style="padding: 16px; font-weight: 700;">₱@account.Balance.ToString("N2")</td>
                    <td style="padding: 16px;">
                        <span style="padding: 4px 12px; background: @(account.Status == "Active" ? "#D4E6D4" : "#fef3c7"); color: @(account.Status == "Active" ? "#2F3E2A" : "#92400e"); border-radius: 8px; font-size: 12px; font-weight: 600;">
                            @account.Status
                        </span>
                    </td>
                    <td style="padding: 16px;">
                        <button class="btn btn-secondary" style="padding: 6px 12px; font-size: 12px;">View</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    private List<CustomerAccount> accounts = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadAccounts();
    }

    private async Task LoadAccounts()
    {
        try
        {
            using var context = await DbContextFactory.CreateDbContextAsync();
            accounts = await context.CustomerAccounts
                .Include(ca => ca.Customer)
                .OrderByDescending(ca => ca.CreatedAt)
                .ToListAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading accounts: {ex.Message}");
        }
    }
}

