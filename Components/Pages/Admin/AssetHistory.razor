@page "/admin/asset-history"
@layout AdminLayout
@using FinanceBank.Models
@using FinanceBank.Services
@using FinanceBank.Data
@using Microsoft.EntityFrameworkCore
@inject AssetMarketplaceService AssetService
@inject AuthService AuthService
@inject NavigationManager Navigation
@inject IDbContextFactory<BFASDbContext> DbContextFactory

<div class="sidebar-content">
    <div class="card">
        <div class="card-header" style="background: linear-gradient(135deg, #5F6F52, #6B7B5E); color: white; padding: 20px;">
            <h4 style="margin: 0; font-weight: 600;">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="margin-right: 8px; vertical-align: middle;">
                    <circle cx="12" cy="12" r="10"></circle>
                    <polyline points="12 6 12 12 16 14"></polyline>
                </svg>
                Asset Application History
            </h4>
            <p style="margin: 8px 0 0 0; opacity: 0.9; font-size: 14px;">Complete audit trail of forwarded and released asset applications with contracts</p>
        </div>

        <div class="card-body" style="padding: 24px;">
            <!-- Filter Section -->
            <div style="background: #f8fafc; padding: 20px; border-radius: 12px; margin-bottom: 24px;">
                <h6 style="margin: 0 0 16px 0; color: #2B2B2B; font-weight: 600;">🔍 Filter History</h6>
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px;">
                    <div>
                        <label style="font-size: 13px; color: #6B6B6B; display: block; margin-bottom: 4px;">Action Type</label>
                        <select @bind="filterActionType" style="width: 100%; padding: 10px; border: 1px solid #E2E2DC; border-radius: 8px; font-size: 14px;">
                            <option value="">All Actions</option>
                            <option value="FORWARDED_TO_ACCOUNTANT">📤 Forwarded to Accountant</option>
                            <option value="FORWARDED_TO_FINANCEMANAGER">📤 Forwarded to Finance Manager</option>
                            <option value="APPROVED">✅ Approved</option>
                            <option value="RELEASED">🎉 Released</option>
                            <option value="REJECTED">❌ Rejected</option>
                        </select>
                    </div>
                    <div>
                        <label style="font-size: 13px; color: #6B6B6B; display: block; margin-bottom: 4px;">From Date</label>
                        <input type="date" @bind="filterFromDate" style="width: 100%; padding: 10px; border: 1px solid #E2E2DC; border-radius: 8px; font-size: 14px;" />
                    </div>
                    <div>
                        <label style="font-size: 13px; color: #6B6B6B; display: block; margin-bottom: 4px;">To Date</label>
                        <input type="date" @bind="filterToDate" style="width: 100%; padding: 10px; border: 1px solid #E2E2DC; border-radius: 8px; font-size: 14px;" />
                    </div>
                    <div>
                        <label style="font-size: 13px; color: #6B6B6B; display: block; margin-bottom: 4px;">Search User</label>
                        <input type="text" @bind="filterUser" placeholder="Username..." style="width: 100%; padding: 10px; border: 1px solid #E2E2DC; border-radius: 8px; font-size: 14px;" />
                    </div>
                </div>
                <div style="margin-top: 16px; display: flex; gap: 12px;">
                    <button @onclick="ApplyFilters" style="padding: 10px 24px; background: #5F6F52; color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600;">
                        Apply Filters
                    </button>
                    <button @onclick="ClearFilters" style="padding: 10px 24px; background: #E2E2DC; color: #2B2B2B; border: none; border-radius: 8px; cursor: pointer; font-weight: 600;">
                        Clear
                    </button>
                </div>
            </div>

            <!-- Statistics Cards -->
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 16px; margin-bottom: 24px;">
                <div style="background: linear-gradient(135deg, #E8F0E8, #D4E6D4); padding: 20px; border-radius: 12px; text-align: center;">
                    <div style="font-size: 28px; font-weight: 700; color: #2F3E2A;">@forwardedToAccountantCount</div>
                    <div style="font-size: 13px; color: #5F6F52;">📤 Forwarded to Accountant</div>
                </div>
                <div style="background: linear-gradient(135deg, #FAF6EE, #F5EBE0); padding: 20px; border-radius: 12px; text-align: center;">
                    <div style="font-size: 28px; font-weight: 700; color: #6B4423;">@forwardedToFMCount</div>
                    <div style="font-size: 13px; color: #8B5A2B;">📤 Forwarded to FM</div>
                </div>
                <div style="background: linear-gradient(135deg, #D4E6D4, #A9B388); padding: 20px; border-radius: 12px; text-align: center;">
                    <div style="font-size: 28px; font-weight: 700; color: #2F3E2A;">@approvedCount</div>
                    <div style="font-size: 13px; color: #5F6F52;">✅ Approved</div>
                </div>
                <div style="background: linear-gradient(135deg, #E4E8DC, #A9B388); padding: 20px; border-radius: 12px; text-align: center;">
                    <div style="font-size: 28px; font-weight: 700; color: #2F3E2A;">@releasedCount</div>
                    <div style="font-size: 13px; color: #6B7B5E;">🎉 Released</div>
                </div>
                <div style="background: linear-gradient(135deg, #FDEDED, #F5D5D5); padding: 20px; border-radius: 12px; text-align: center;">
                    <div style="font-size: 28px; font-weight: 700; color: #7B2A2A;">@rejectedCount</div>
                    <div style="font-size: 13px; color: #9B3A3A;">❌ Rejected</div>
                </div>
            </div>

            @if (isLoading)
            {
                <div style="text-align: center; padding: 40px;">
                    <div style="width: 40px; height: 40px; border: 3px solid #E2E2DC; border-top-color: #5F6F52; border-radius: 50%; animation: spin 1s linear infinite; margin: 0 auto;"></div>
                    <p style="margin-top: 16px; color: #6B6B6B;">Loading history...</p>
                </div>
            }
            else if (historyList == null || historyList.Count == 0)
            {
                <div style="text-align: center; padding: 60px 20px; background: #FAFAF8; border-radius: 12px;">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="#9B9B9B" stroke-width="1.5" style="margin: 0 auto;">
                        <circle cx="12" cy="12" r="10"></circle>
                        <polyline points="12 6 12 12 16 14"></polyline>
                    </svg>
                    <h5 style="color: #6B6B6B; margin-top: 16px;">No History Found</h5>
                    <p style="color: #9B9B9B; font-size: 14px;">No workflow events match your filters.</p>
                </div>
            }
            else
            {
                <div style="overflow-x: auto;">
                    <table style="width: 100%; border-collapse: collapse;">
                        <thead>
                            <tr style="background: #FAFAF8; border-bottom: 2px solid #E2E2DC;">
                                <th style="padding: 12px; text-align: left; font-weight: 600; color: #2B2B2B;">Date/Time</th>
                                <th style="padding: 12px; text-align: left; font-weight: 600; color: #2B2B2B;">Application</th>
                                <th style="padding: 12px; text-align: left; font-weight: 600; color: #2B2B2B;">Asset</th>
                                <th style="padding: 12px; text-align: center; font-weight: 600; color: #2B2B2B;">Action</th>
                                <th style="padding: 12px; text-align: left; font-weight: 600; color: #2B2B2B;">Performed By</th>
                                <th style="padding: 12px; text-align: left; font-weight: 600; color: #2B2B2B;">Contract</th>
                                <th style="padding: 12px; text-align: right; font-weight: 600; color: #2B2B2B;">Amount</th>
                                <th style="padding: 12px; text-align: center; font-weight: 600; color: #2B2B2B;">Details</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var history in historyList)
                            {
                                <tr style="border-bottom: 1px solid #E2E2DC; transition: background 0.2s;" class="table-row-hover">
                                    <td style="padding: 16px 12px;">
                                        <div style="font-weight: 600; color: #2B2B2B;">@history.ActionDate.ToString("MMM dd, yyyy")</div>
                                        <div style="font-size: 12px; color: #6B6B6B;">@history.ActionDate.ToString("hh:mm tt")</div>
                                    </td>
                                    <td style="padding: 16px 12px;">
                                        <span style="background: #E8F0E8; color: #2F3E2A; padding: 4px 12px; border-radius: 20px; font-weight: 600; font-size: 12px;">
                                            @history.Application?.ApplicationNumber
                                        </span>
                                    </td>
                                    <td style="padding: 16px 12px;">
                                        <div style="font-weight: 600; color: #2B2B2B;">@(history.Application?.Asset?.DisplayTitle ?? "N/A")</div>
                                        <div style="font-size: 12px; color: #6B6B6B;">@history.Application?.Asset?.AssetType</div>
                                    </td>
                                    <td style="padding: 16px 12px; text-align: center;">
                                        @{
                                            var (actionColor, actionBg, actionIcon) = GetActionStyle(history.ActionType);
                                        }
                                        <span style="background: @actionBg; color: @actionColor; padding: 6px 14px; border-radius: 20px; font-size: 12px; font-weight: 600;">
                                            @actionIcon @GetActionLabel(history.ActionType)
                                        </span>
                                    </td>
                                    <td style="padding: 16px 12px;">
                                        <div style="font-weight: 600; color: #2B2B2B;">@history.ActionBy</div>
                                        <div style="font-size: 12px; color: #6B6B6B;">@history.ActionByRole</div>
                                    </td>
                                    <td style="padding: 16px 12px;">
                                        @if (!string.IsNullOrEmpty(history.ContractNumber))
                                        {
                                            <div style="font-weight: 600; color: #5F6F52;">@history.ContractNumber</div>
                                            <div style="font-size: 12px; color: #6B6B6B;">@history.ContractDate?.ToString("MM/dd/yyyy")</div>
                                        }
                                        else
                                        {
                                            <span style="color: #9B9B9B; font-size: 12px;">-</span>
                                        }
                                    </td>
                                    <td style="padding: 16px 12px; text-align: right;">
                                        @if (history.AssetPrice > 0)
                                        {
                                            <div style="font-weight: 700; color: #2B2B2B;">₱@history.AssetPrice?.ToString("N2")</div>
                                            @if (history.LoanAmount > 0)
                                            {
                                                <div style="font-size: 12px; color: #6B6B6B;">Loan: ₱@history.LoanAmount?.ToString("N2")</div>
                                            }
                                        }
                                        else
                                        {
                                            <span style="color: #9B9B9B;">-</span>
                                        }
                                    </td>
                                    <td style="padding: 16px 12px; text-align: center;">
                                        <button @onclick="() => ShowHistoryDetails(history)" 
                                            style="padding: 8px 16px; background: #5F6F52; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 13px; font-weight: 500;">
                                            View
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>

<!-- History Details Modal -->
@if (showDetailsModal && selectedHistory != null)
{
    <div style="position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.5); display: flex; align-items: center; justify-content: center; z-index: 1000;" @onclick="CloseModal">
        <div style="background: white; border-radius: 16px; width: 90%; max-width: 700px; max-height: 90vh; overflow-y: auto; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25);" @onclick:stopPropagation>
            <div style="padding: 24px; border-bottom: 1px solid #E2E2DC; background: linear-gradient(135deg, #5F6F52, #6B7B5E); border-radius: 16px 16px 0 0;">
                <h4 style="margin: 0; color: white; font-weight: 600;">📜 History Details</h4>
                <p style="margin: 8px 0 0 0; color: rgba(255,255,255,0.9); font-size: 14px;">@selectedHistory.Application?.ApplicationNumber</p>
            </div>
            
            <div style="padding: 24px;">
                <!-- Action Info -->
                <div style="background: #f8fafc; padding: 20px; border-radius: 12px; margin-bottom: 20px;">
                    <h6 style="margin: 0 0 16px 0; color: #2B2B2B; font-weight: 600;">Action Information</h6>
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 16px;">
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Action Type</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@GetActionLabel(selectedHistory.ActionType)</div>
                        </div>
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Date & Time</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.ActionDate.ToString("MMM dd, yyyy hh:mm tt")</div>
                        </div>
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Performed By</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.ActionBy</div>
                        </div>
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Role</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.ActionByRole</div>
                        </div>
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Previous Status</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@(selectedHistory.PreviousStatus ?? "-")</div>
                        </div>
                        <div>
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">New Status</label>
                            <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@(selectedHistory.NewStatus ?? "-")</div>
                        </div>
                    </div>
                    @if (!string.IsNullOrEmpty(selectedHistory.Remarks))
                    {
                        <div style="margin-top: 16px;">
                            <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Remarks</label>
                            <div style="font-weight: 500; color: #2B2B2B; margin-top: 4px; padding: 12px; background: white; border-radius: 8px; border: 1px solid #E2E2DC;">
                                @selectedHistory.Remarks
                            </div>
                        </div>
                    }
                </div>

                <!-- Contract Info (if released) -->
                @if (!string.IsNullOrEmpty(selectedHistory.ContractNumber))
                {
                    <div style="background: #E8F0E8; padding: 20px; border-radius: 12px; margin-bottom: 20px; border: 1px solid #A9B388;">
                        <h6 style="margin: 0 0 16px 0; color: #2F3E2A; font-weight: 600;">📄 Contract Information</h6>
                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 16px;">
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Contract Number</label>
                                <div style="font-weight: 700; color: #5F6F52; margin-top: 4px; font-size: 16px;">@selectedHistory.ContractNumber</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Contract Date</label>
                                <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.ContractDate?.ToString("MMM dd, yyyy")</div>
                            </div>
                            @if (!string.IsNullOrEmpty(selectedHistory.DeedOfSaleNumber))
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Deed of Sale</label>
                                    <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.DeedOfSaleNumber</div>
                                </div>
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Deed Date</label>
                                    <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.DeedOfSaleDate?.ToString("MMM dd, yyyy")</div>
                                </div>
                            }
                            @if (!string.IsNullOrEmpty(selectedHistory.ORNumber))
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">OR Number</label>
                                    <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.ORNumber</div>
                                </div>
                            }
                            @if (!string.IsNullOrEmpty(selectedHistory.CRNumber))
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">CR Number</label>
                                    <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.CRNumber</div>
                                </div>
                            }
                        </div>
                    </div>
                }

                <!-- Financial Details -->
                @if (selectedHistory.AssetPrice > 0)
                {
                    <div style="background: #FAF6EE; padding: 20px; border-radius: 12px; margin-bottom: 20px; border: 1px solid #E5D9B6;">
                        <h6 style="margin: 0 0 16px 0; color: #6B4423; font-weight: 600;">💰 Financial Snapshot</h6>
                        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;">
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Asset Price</label>
                                <div style="font-weight: 700; color: #2B2B2B; margin-top: 4px;">₱@selectedHistory.AssetPrice?.ToString("N2")</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Down Payment</label>
                                <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">₱@selectedHistory.DownPaymentAmount?.ToString("N2")</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Loan Amount</label>
                                <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">₱@selectedHistory.LoanAmount?.ToString("N2")</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Term</label>
                                <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.TermMonths months</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Interest Rate</label>
                                <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.InterestRate?.ToString("N2")%</div>
                            </div>
                            <div>
                                <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Monthly Payment</label>
                                <div style="font-weight: 700; color: #5F6F52; margin-top: 4px;">₱@selectedHistory.MonthlyPayment?.ToString("N2")</div>
                            </div>
                        </div>
                    </div>
                }

                <!-- Assessment Details (if available) -->
                @if (selectedHistory.DebtToIncomeRatio.HasValue || !string.IsNullOrEmpty(selectedHistory.CreditRiskLevel))
                {
                    <div style="background: #E4E8DC; padding: 20px; border-radius: 12px; margin-bottom: 20px; border: 1px solid #A9B388;">
                        <h6 style="margin: 0 0 16px 0; color: #2F3E2A; font-weight: 600;">📊 Assessment Details</h6>
                        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;">
                            @if (selectedHistory.DebtToIncomeRatio.HasValue)
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Debt-to-Income</label>
                                    <div style="font-weight: 600; color: #2B2B2B; margin-top: 4px;">@selectedHistory.DebtToIncomeRatio?.ToString("N2")%</div>
                                </div>
                            }
                            @if (!string.IsNullOrEmpty(selectedHistory.CreditRiskLevel))
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Credit Risk</label>
                                    <div style="font-weight: 600; color: @GetRiskColor(selectedHistory.CreditRiskLevel); margin-top: 4px;">@selectedHistory.CreditRiskLevel</div>
                                </div>
                            }
                            @if (!string.IsNullOrEmpty(selectedHistory.Recommendation))
                            {
                                <div>
                                    <label style="font-size: 12px; color: #6B6B6B; text-transform: uppercase;">Recommendation</label>
                                    <div style="font-weight: 600; color: @(selectedHistory.Recommendation == "APPROVE" ? "#5F6F52" : "#9B3A3A"); margin-top: 4px;">@selectedHistory.Recommendation</div>
                                </div>
                            }
                        </div>
                    </div>
                }

                @if (selectedHistory.LinkedLoanId.HasValue)
                {
                    <div style="background: #E8F0E8; padding: 16px; border-radius: 12px; text-align: center;">
                        <span style="color: #2F3E2A; font-weight: 600;">📋 Linked Loan ID: @selectedHistory.LinkedLoanId</span>
                    </div>
                }

                <div style="text-align: right; margin-top: 20px;">
                    <button @onclick="CloseModal" style="padding: 12px 32px; background: #5F6F52; color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600;">
                        Close
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<style>
    .table-row-hover:hover {
        background: #FAFAF8;
    }
    
    @@keyframes spin {
        to { transform: rotate(360deg); }
    }
</style>

@code {
    private List<FinanceBank.Models.AssetHistory>? historyList;
    private List<FinanceBank.Models.AssetHistory>? allHistory;
    private bool isLoading = true;
    
    // Filter fields
    private string filterActionType = "";
    private DateTime? filterFromDate;
    private DateTime? filterToDate;
    private string filterUser = "";
    
    // Modal
    private bool showDetailsModal = false;
    private FinanceBank.Models.AssetHistory? selectedHistory;
    
    // Statistics
    private int forwardedToAccountantCount => allHistory?.Count(h => h.ActionType == "FORWARDED_TO_ACCOUNTANT") ?? 0;
    private int forwardedToFMCount => allHistory?.Count(h => h.ActionType == "FORWARDED_TO_FINANCEMANAGER") ?? 0;
    private int approvedCount => allHistory?.Count(h => h.ActionType == "APPROVED") ?? 0;
    private int releasedCount => allHistory?.Count(h => h.ActionType == "RELEASED") ?? 0;
    private int rejectedCount => allHistory?.Count(h => h.ActionType == "REJECTED") ?? 0;

    protected override async Task OnInitializedAsync()
    {
        await LoadHistory();
    }

    private async Task LoadHistory()
    {
        isLoading = true;
        try
        {
            allHistory = await AssetService.GetAllHistoryAsync();
            historyList = allHistory;
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading history: {ex.Message}");
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task ApplyFilters()
    {
        isLoading = true;
        try
        {
            historyList = await AssetService.GetAllHistoryAsync(
                filterFromDate, 
                filterToDate, 
                string.IsNullOrEmpty(filterActionType) ? null : filterActionType,
                string.IsNullOrEmpty(filterUser) ? null : filterUser);
        }
        finally
        {
            isLoading = false;
        }
    }

    private void ClearFilters()
    {
        filterActionType = "";
        filterFromDate = null;
        filterToDate = null;
        filterUser = "";
        historyList = allHistory;
    }

    private void ShowHistoryDetails(FinanceBank.Models.AssetHistory history)
    {
        selectedHistory = history;
        showDetailsModal = true;
    }

    private void CloseModal()
    {
        showDetailsModal = false;
        selectedHistory = null;
    }

    private (string color, string bg, string icon) GetActionStyle(string actionType)
    {
        return actionType switch
        {
            "FORWARDED_TO_ACCOUNTANT" => ("#2F3E2A", "#E8F0E8", "📤"),
            "FORWARDED_TO_FINANCEMANAGER" => ("#6B4423", "#FAF6EE", "📤"),
            "APPROVED" => ("#2F3E2A", "#D4E6D4", "✅"),
            "RELEASED" => ("#2F3E2A", "#E4E8DC", "🎉"),
            "REJECTED" => ("#7B2A2A", "#FDEDED", "❌"),
            "SUBMITTED" => ("#6B6B6B", "#F7F7F4", "📋"),
            _ => ("#2B2B2B", "#F7F7F4", "📄")
        };
    }

    private string GetActionLabel(string actionType)
    {
        return actionType switch
        {
            "FORWARDED_TO_ACCOUNTANT" => "Forwarded to Accountant",
            "FORWARDED_TO_FINANCEMANAGER" => "Forwarded to Finance Manager",
            "APPROVED" => "Approved",
            "RELEASED" => "Released",
            "REJECTED" => "Rejected",
            "SUBMITTED" => "Submitted",
            "CANCELLED" => "Cancelled",
            _ => actionType
        };
    }

    private string GetRiskColor(string? riskLevel)
    {
        return riskLevel?.ToUpper() switch
        {
            "LOW" => "#5F6F52",
            "MEDIUM" => "#8B5A2B",
            "HIGH" => "#9B3A3A",
            _ => "#6B6B6B"
        };
    }
}
