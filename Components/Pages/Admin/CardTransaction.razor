@page "/admin/transactions/cards"
@page "/customer/my-cards"
@layout AdminLayout

<div class="section-header">
    <h2 class="section-title">💳 Card Transaction Management</h2>
    <p style="color: var(--text-secondary); font-size: 14px; margin-top: 8px;">
        Manage debit and credit card transactions
    </p>
</div>

<div style="background: white; border-radius: 16px; padding: 32px; border: 1px solid var(--border);">
    <form>
        <!-- Card Information -->
        <div style="margin-bottom: 32px;">
            <h4 style="font-size: 15px; font-weight: 700; margin-bottom: 16px; background: #fef3c7; padding: 12px; border-radius: 8px;">
                💳 Card Information
            </h4>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                <div class="form-group">
                    <label class="form-label required-field">Card Number (Masked)</label>
                    <input type="text" class="form-control" placeholder="**** **** **** 1234" style="font-family: monospace; font-weight: 700;" />
                </div>
                <div class="form-group">
                    <label class="form-label required-field">Card Type</label>
                    <select class="form-control">
                        <option>💳 Debit Card</option>
                        <option>💎 Credit Card</option>
                        <option>🌐 Prepaid Card</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="form-label required-field">Card Holder Name</label>
                    <input type="text" class="form-control" placeholder="Name as on card" />
                </div>
                <div class="form-group">
                    <label class="form-label">Expiry Date</label>
                    <input type="text" class="form-control" placeholder="MM/YY" />
                </div>
            </div>
        </div>

        <!-- Transaction Details -->
        <div style="margin-bottom: 32px;">
            <h4 style="font-size: 15px; font-weight: 700; margin-bottom: 16px; background: #fef3c7; padding: 12px; border-radius: 8px;">
                📋 Transaction Details
            </h4>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                <div class="form-group">
                    <label class="form-label">Transaction ID</label>
                    <input type="text" class="form-control" value="CARD-TXN-20251112001" readonly style="background: #f3f4f6; font-family: monospace;" />
                </div>
                <div class="form-group">
                    <label class="form-label">Transaction Date</label>
                    <input type="datetime-local" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="form-label required-field">Merchant Name</label>
                    <input type="text" class="form-control" placeholder="Store or merchant name" />
                </div>
                <div class="form-group">
                    <label class="form-label">Merchant Category</label>
                    <select class="form-control">
                        <option>🛒 Retail/Shopping</option>
                        <option>🍔 Food & Dining</option>
                        <option>⛽ Gas Station</option>
                        <option>✈️ Travel</option>
                        <option>🏥 Healthcare</option>
                        <option>💻 Online Purchase</option>
                        <option>🎬 Entertainment</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="form-label required-field">Transaction Amount (₱)</label>
                    <input type="number" class="form-control" placeholder="0.00" style="font-size: 18px; font-weight: 700; color: #dc2626;" />
                </div>
                <div class="form-group">
                    <label class="form-label required-field">Status</label>
                    <select class="form-control">
                        <option>✅ Approved</option>
                        <option>⏳ Pending</option>
                        <option>❌ Declined</option>
                        <option>🔄 Processing</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- Card Limits -->
        <div style="margin-bottom: 32px;">
            <h4 style="font-size: 15px; font-weight: 700; margin-bottom: 16px; background: #fef3c7; padding: 12px; border-radius: 8px;">
                🎯 Card Limits & Usage
            </h4>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                <div class="form-group">
                    <label class="form-label">Daily Transaction Limit</label>
                    <input type="text" class="form-control" value="₱100,000.00" readonly style="background: #f3f4f6;" />
                </div>
                <div class="form-group">
                    <label class="form-label">Monthly Credit Limit</label>
                    <input type="text" class="form-control" value="₱500,000.00" readonly style="background: #f3f4f6;" />
                </div>
                <div class="form-group">
                    <label class="form-label">Current Balance</label>
                    <input type="text" class="form-control" value="₱85,450.25" readonly style="background: #fee2e2; font-weight: 700; color: #991b1b;" />
                </div>
                <div class="form-group">
                    <label class="form-label">Available Credit</label>
                    <input type="text" class="form-control" value="₱414,549.75" readonly style="background: #d1fae5; font-weight: 700; color: #065f46;" />
                </div>
            </div>

            <!-- Limit Usage Progress -->
            <div style="margin-top: 16px; padding: 16px; background: #fef3c7; border-radius: 8px;">
                <div style="display: flex; justify-content: space-between; margin-bottom: 8px;">
                    <span style="font-size: 13px; font-weight: 600;">Credit Utilization</span>
                    <span style="font-size: 13px; font-weight: 700; color: #f59e0b;">17.1%</span>
                </div>
                <div style="height: 8px; background: #e5e7eb; border-radius: 4px; overflow: hidden;">
                    <div style="height: 100%; width: 17.1%; background: linear-gradient(90deg, #f59e0b, #d97706);"></div>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div style="display: flex; gap: 12px; justify-content: flex-end;">
            <button type="button" class="btn" style="background: #f3f4f6; color: #374151; padding: 12px 24px;">Reset</button>
            <button type="button" class="btn" style="background: #f59e0b; color: white; padding: 12px 32px;">Process Transaction</button>
        </div>
    </form>
</div>

@code {
    // UI Only - No backend logic
}
